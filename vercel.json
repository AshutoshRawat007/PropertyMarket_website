{
  "buildCommand": "cd client && npm install && npm build",
  "outputDirectory": "client/build",
  "framework": "vite",
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/api/index.js"
    },
    {"source": "/(.*)", "destination": "/"}
  ]
}